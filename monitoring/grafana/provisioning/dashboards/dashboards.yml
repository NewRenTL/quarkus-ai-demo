apiVersion: 1

providers:
  - name: 'quarkus-vs-spring'
    orgId: 1
    folder: 'Demo — Quarkus vs Spring'
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    options:
      path: /etc/grafana/provisioning/dashboards
